# Lobby Guide overlay — RETIRED

**Status:** Hidden from the customer path (2026-07-18)

The cream Lobby Guide panel (`GuideConversationPanel`, `LobbyGuideOverlay`,
`GuideSsrBootShell`, `?guide=1`) is no longer mounted.

**Live path:** Studio Lobby podium → `/studio-conversation-room`

**Legacy URL:** `/?guide=1` and `/studio-lobby?guide=1` redirect to
`/studio-conversation-room`.

These files remain in place as a reference archive until Tagia approves a
physical move under `src/archive/`. Do not re-mount on Lobby pages.
