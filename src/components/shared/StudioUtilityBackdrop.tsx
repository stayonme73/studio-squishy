/** Fixed full-viewport Studio Lobby backdrop for utility pages (Project Summary, Secure Checkout). */
export default function StudioUtilityBackdrop() {
  return <div className="studio-utility-backdrop" aria-hidden="true" />;
}
