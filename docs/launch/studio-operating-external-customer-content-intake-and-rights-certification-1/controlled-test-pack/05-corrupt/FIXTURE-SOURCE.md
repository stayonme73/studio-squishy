# Case 5 — synthetic corrupt PNG

**Filename:** `northwind-corrupt.png`

This is a truncated PNG header (10 bytes). It is **not** a customer asset. Scout generated it for Gate X Case 5 technical rejection only.

Bytes match `SYNTHETIC_CORRUPT_PNG_BYTES` in `src/lib/studio-customer-content-intake/test-fixtures.ts`.
