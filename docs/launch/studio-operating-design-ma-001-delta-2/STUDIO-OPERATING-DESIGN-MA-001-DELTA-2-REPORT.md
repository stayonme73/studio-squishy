# STUDIO-OPERATING-DESIGN-MA-001-DELTA-2 REPORT

**Package:** STUDIO-OPERATING-DESIGN-MA-001-DELTA-2  
**Mode:** Technical re-inspect only — against Owner-accepted CONTRACT-TRUTH-1 (with clarifications) · no implementation · no proof · no remapping  
**Scout status:** PARKED  
**Final status:** READY FOR OWNER REVIEW  
**Git:** No commit · No push · No merge  

---

## Verdict

### MA-001 DELTA C — HETEROGENEOUS PACK ORCHESTRATION (HOLDS; NOT PROMOTED TO B)

Against the **accepted** product truth (closed kinds · `lockedPackMemberCount` ∈ {1,2,3,4} · customer-locked composition · member≠file · inherited member content contracts · exact N/N · pack QA/manifest/versioning), remaining Machine work is **still a material new capability class** — not a small extension of one sealed spine.

CONTRACT-TRUTH-1 retired the undefined-product holes that made DELTA-1 **C** (open “similar,” undefined N/chooser, undefined pack identity). That does **not** automatically earn **B**.

What remains is not “parameterize social for three discrete sizes.” It is:

1. A **heterogeneous pack orchestrator** — one job fans out to **different** sealed producers (flyer / menu / service-sheet / business-card / single promo surface), fans in under one pack root, and fails closed on **member** completeness  
2. A **single `promotion_graphic` adapter** — sealed promo proves **exact-two** coordinated graphics (`PROMO_PROOF_CONTRACT.exactAssetCount: 2`), not one arbitrary standalone pack member  
3. Pack-level identity, QA, and delivery packaging that treat **member identities** as the N unit while allowing multi-artifact members

Not A: product bounds are frozen; four member producers already sealed; set doctrine exists on promo/social.  
Not B: would be a **courtesy promotion** — no sealed lane is a multi-kind pack orchestrator; single-promo path is unproven; sm-001’s B was earned by specializing **one** spine.  
Not D: still create-from-campaign Machine path.

**Expectation check:** Shrink C→B was possible in principle after freeze; **orchestration + single-promo adapter did not earn B** on this reinspect.

---

## 1. Starting control point

| Field | Value |
|-------|--------|
| Control SHA | `30b0e4ddef1f54813d5a408d12c28e26dccd4f22` |
| Product authority | CONTRACT-TRUTH-1 **Owner accepted with two clarifications** |
| Prior technical class | DELTA-1 → **C** (product-undefined drivers) |
| This reinspect | Reclass against **frozen** product — still **C** for engineering delta |
| Sealed lanes | flyer · business-card · menu · service-sheet · promotion-graphics · social-posts · sm-001 · sm-001-monthly (**8/13**) |
| Candidate | `ma-001` — still Canva · unmapped · proof **not** authorized |

---

## 2. Frozen product truth (inputs to this inspect)

| Law | Content |
|-----|---------|
| Closed kinds | `flyer` · `menu` · `service_sheet` · `business_card` · `promotion_graphic` only |
| Unsupported / “similar” | Fail closed **before payment**; no silent substitute |
| N | `lockedPackMemberCount` ∈ **{1,2,3,4}** |
| Member vs file (Amendment 1) | N counts **member identities**; each member may own multiple artifacts per sealed producer |
| Mix | Mixed kinds allowed; same kind repeated allowed; no nested multi-asset SKU as one slot |
| Chooser | Customer locks `plannedPackMembers` pre-payment; Studio may recommend inside enum only |
| Plates | Inherit sealed executables; promo Landscape fail-closed |
| Copy/content (Amendment 2) | **No** new pack copy service; each member **inherits** sealed producer content/copy contract |
| Completeness | Exact **N/N** on members; QA must not shrink N |
| Delivery | Whole-pack `vN` + pack manifest + member artifacts |
| Not in product | Social captions · calendar · posting order · open “similar” kinds |

---

## 3. Eight-lane baseline — what is already proven

| Capability | Proven by | Reuse for `ma-001`? |
|------------|-----------|---------------------|
| Flyer / menu / service-sheet single-surface | Sealed singles | **Yes** as **member producers** under pack root |
| Business card multi-surface (front/back) | Business card | **Yes** as **one member** with multiple artifacts (Amendment 1) |
| Exact-2 same-SKU campaign set · mixed plates · set QA | Promotion graphics | **Pattern** for pack QA/versioning — **not** a heterogeneous kind pack; **not** a single-graphic member API |
| Same-kind variable N · exact N/N · whole-set versioning | sm-001 / monthly | **Pattern** for member-count lock — **same kind only**; captions/calendar **out of scope** |
| Square / portrait promo plates | Promo | **Yes** for `promotion_graphic` plate choice (Landscape fail-closed) |
| Live dispatch / idempotency / observer | All eight (SKU-gated) | **Pattern only** — `ma-001` unwired |
| Owner routine NONE | Sealed lanes | **Preserve** |

**Explicitly not proven:** multi-kind pack orchestration · `lockedPackMemberCount` / `plannedPackMembers` job identity · pack manifest for heterogeneous members · single-graphic promo member path · customer pre-payment composition lock on Machine intake · `ma-001` dispatch.

---

## 4. Genuinely new engineering (honest list)

| # | New engineering | Why it is new | Size |
|---|-----------------|---------------|------|
| 1 | **Pack composition identity** | No sealed lane carries customer-locked heterogeneous `plannedPackMembers` + `lockedPackMemberCount` (member≠file) | Small–medium — types + immutability |
| 2 | **Heterogeneous pack orchestrator** | No sealed job fans out to **different** producer pipelines and commits one atomic pack | **Large** — core C driver |
| 3 | **Member-level invoke adapters** | Flyer/menu/sheet/card pipelines are SKU jobs today, not pack-member subordinates | Medium — wrap without lying that each is a standalone SKU purchase |
| 4 | **Single `promotion_graphic` adapter** | `exactAssetCount: 2` promo contract / `setIncompleteUnlessBothPass` — cannot call promo job as one pack member | **Medium–large** — Owner-acknowledged genuine new |
| 5 | **Pack-level QA** | CERT wants multi-asset consistency for `ma-001`; Machine has no cross-kind pack gate that keys off member IDs | Medium |
| 6 | **Whole-pack versioning + delivery manifest** | Promo/social whole-set doctrine exists; heterogeneous member manifest (kind/purpose/plate/artifacts) does not | Medium |
| 7 | **Content-contract inheritance wiring** | Not new copy product (Amendment 2) — still need per-member brief/map to sealed content rules | Small–medium |
| 8 | **Pre-payment composition lock + unsupported fail-closed** | Product law exists; Machine intake/composition UX not built | Later execution packages — classify render delta without requiring full intake UI now |

**Not new:** Individual sealed compose for the four singles · promo square/portrait plates · brand/campaign fact-lock doctrine · export primitives · observer/idempotency patterns · Canva-off / Make-not-required pattern after authorized proof+hook · “no social captions / no calendar” (absence is product law, not new build).

---

## 5. Why this is not B (anti-courtesy)

| B-pattern (sm-001 DELTA-2) | `ma-001` reality |
|----------------------------|------------------|
| Specialize **one** sealed spine (social exact-4 → {4,5,6}) | Must coordinate **five kind paths** under one job |
| Add one sibling artifact (calendar) on same set | Add **pack shell** whose members are different SKU shapes |
| Same plate family | Mixed plates **and** mixed producer contracts |
| Caption/order engines already on that spine | Content law is **per-member inheritance**, not one shared caption engine |
| No new “half of a sealed set” product | **Must** invent single-graphic path beside sealed exact-2 promo |

Calling this **B** would treat “glue exists somewhere” as “glue is proven.” It is not.

**Watchpath to B (not earned yet):** Only if a later Owner-accepted scope cut removes `promotion_graphic` from V1 **and** proof shows pack orchestration collapses to thin deterministic wrappers with no new failure classes — then reinspect. Do **not** pre-award that.

---

## 6. Failure modes if forced as “small extension”

| Failure mode | How it appears |
|--------------|----------------|
| File-count N | Card front+back or PNG+PDF counted as two members → false incompleteness or false over-count |
| Promo dual nest | Exact-2 promo job stuffed into one slot → hidden second asset or forced pair |
| SKU-job leakage | Each member billed/dispatched as separate RTU purchase → pack identity lost |
| Partial pack success | Three of four members ship because “most Canva jobs passed” |
| Content override | Universal “final copy” slogan skips menu/service-sheet sealed content gates |
| Kind invention | “Similar” or poster sneaks in mid-orchestration |

---

## 7. Delta class rationale

| Class | Decision |
|-------|----------|
| **A** | Rejected — product frozen; member producers exist; set doctrines exist as patterns |
| **B** | Rejected — orchestration + single-promo adapter are material; not one-spine parameterization; courtesy B refused |
| **C** | **Accepted** — heterogeneous pack layer + single-promo member path remain the principal new capability after contract freeze |
| **D** | Rejected — not edit/ingest |

**Collapse path:** DELTA-1 **C** (undefined product) → CONTRACT-TRUTH-1 accepted → DELTA-2 **still C** (defined product; material engineering remains).

Product freeze **did** its job: Machine can now be designed against closed law. Class did not shrink because the remaining work was never “fill in N and calendar on the same engine.”

---

## 8. Protected lanes / non-goals

| Preserve | Status this package |
|----------|---------------------|
| Eight sealed design lanes | Untouched |
| `ma-001` unmapped / Canva | Confirmed |
| Proof / remap / commit / push / merge | **None** |
| Make | NOT REQUIRED |
| `ma-001-monthly` | Parked |

---

## 9. Exactly one recommended next step

**`STUDIO-OPERATING-DESIGN-MA-001-PROOF-1`**

Only after Owner **accepts this DELTA-2 class (C)** and **explicitly authorizes proof**.

Bounded first proof should demonstrate — without remapping Canva yet unless Owner includes remap in that authorization:

1. Customer-locked composition with `lockedPackMemberCount` / `plannedPackMembers`  
2. Heterogeneous pack orchestration (at least one mixed-kind fixture)  
3. Member≠file accounting (include a `business_card` member)  
4. Exact N/N pack fail-closed (no shrink)  
5. Pack manifest + whole-pack versioning  
6. Single `promotion_graphic` member path (square or portrait only) **or** Owner-scoped exclusion of promo from first proof with explicit follow-on  

Until that authorization, do **not** start proof, intake wiring, remap, commit, push, or merge.

---

## READY FOR OWNER REVIEW

**Scout PARKED.**
